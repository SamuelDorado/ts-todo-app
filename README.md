# ts-boilerplate

This is a boilerplate for developing using TypeScript + Webpack.

## Usage
```sh

$ yarn install

# run tsc server and webpack-dev-server
$ yarn start
#=> http://localhost:8080

# run linter
$ yarn lint
# with fix
$ yarn lint:fix

# build
$ yarn build

```
